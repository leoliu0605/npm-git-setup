# @leoli0605/git-setup

## 0.2.0

- Add `git config --global svn.rmdir true` to remove empty folders when `git svn dcommit`
- Adjust `git config --global alias.st status` to `git config --global alias.ss status`
- Remove `git config --global core.editor notepad` because it's not necessary

## 0.1.0

- Initial release
